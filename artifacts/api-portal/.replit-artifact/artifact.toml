kind = "web"
previewPath = "/"
title = "API Portal"
version = "1.0.0"
id = "artifacts/api-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24927

[services.development]
run = "pnpm --filter @workspace/api-portal run dev"

[services.env]
PORT = "24927"
BASE_PATH = "/"
